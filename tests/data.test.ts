import{describe,it,expect}from'vitest';import{projects}from'../src/data/projects';describe('project data',()=>{it('has unique ids and slugs',()=>{expect(new Set(projects.map(p=>p.id)).size).toBe(projects.length);expect(new Set(projects.map(p=>p.slug)).size).toBe(projects.length)});it('marks every demo explicitly',()=>{expect(projects.every(p=>p.placeholder&&p.summary.includes('DEMO PLACEHOLDER'))).toBe(true)})});
