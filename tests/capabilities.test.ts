import{describe,it,expect}from'vitest';import{capabilities}from'../src/data/capabilities';
describe('capability data',()=>{it('uses unique ids and evidence-based levels',()=>{expect(new Set(capabilities.map(c=>c.id)).size).toBe(capabilities.length);for(const capability of capabilities){expect(capability.evidence.length).toBeGreaterThan(0);expect(['Learning','Familiar','Practical Experience','Project Experience']).toContain(capability.level);expect(capability.summary.length).toBeGreaterThan(15)}});it('does not claim production experience',()=>{expect(capabilities.some(c=>String(c.level).includes('Production'))).toBe(false)})});
